import { LikeanddislikeDirective } from './likeanddislike.directive';

describe('LikeanddislikeDirective', () => {
  it('should create an instance', () => {
    const directive = new LikeanddislikeDirective();
    expect(directive).toBeTruthy();
  });
});
