import { MostLikesDirective } from './most-likes.directive';

describe('MostLikesDirective', () => {
  it('should create an instance', () => {
    const directive = new MostLikesDirective();
    expect(directive).toBeTruthy();
  });
});
